import UIConfig from "../types/configs/UI";

const uiConfig: UIConfig = {
  defaultThemePalette: "dark",
  lightThemePalette: "light",
  darkThemePalette: "dark",
  snackbarCloseTimeInMS: 3000,
};

export default uiConfig;
